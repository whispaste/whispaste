## 2024-08-14 - Existing translations as quick-wins\n**Learning:** Reusing an existing, broader L10n string like `historyCopiedToClipboard` (even outside of history context) avoids the need to touch translation files and re-run code generation for small features.\n**Action:** Prefer existing l10n strings (or generic ones like `actionCopy`) for tiny Sprout features to keep the blast radius confined purely to Dart files.
