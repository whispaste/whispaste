## 2025-02-23 - Remember last export format
**Learning:** Adding a simple `SharedPreferences` lookup to a modal picker significantly reduces repetitive user actions, aligning perfectly with Sprout's goal of small quality-of-life enhancements.
**Action:** Always look for opportunities to persist user selections for repetitive actions using existing mechanisms like `SharedPreferences`.
