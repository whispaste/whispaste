---
import SectionEyebrow from './SectionEyebrow.astro';
import { t } from '../scripts/i18n.ts';

const lang = (Astro.currentLocale === 'en' ? 'en' : 'de') as 'de' | 'en';
const isEn = lang === 'en';

const workflows = [
  {
    key: 'developer',
    href: isEn ? '/en/use-cases/programmer/' : '/use-cases/programmierer/',
    illustration: '/illustrations/use-case-developer.svg',
    illustrationAltKey: 'useCase.programmer.illustrationAlt',
  },
  {
    key: 'students',
    href: isEn ? '/en/use-cases/students/' : '/use-cases/studierende/',
    illustration: '/illustrations/use-case-students.svg',
    illustrationAltKey: 'useCase.students.illustrationAlt',
  },
  {
    key: 'everyday',
    href: isEn ? '/en/use-cases/everyday/' : '/use-cases/vielschreiber/',
    illustration: '/illustrations/use-case-everyday.svg',
    illustrationAltKey: 'useCase.everyday.illustrationAlt',
  },
] as const;
---

<!-- UseCaseCards Section — Zielgruppen-Einstieg nach "How it works" -->
<section id="use-cases" class="band band-raised py-24 sm:py-32 relative scroll-mt-20">
  <div class="relative max-w-5xl mx-auto px-6">
    <div class="text-center mb-14">
      <SectionEyebrow key="useCaseCards.label" class="reveal-up" />
      <h2 class="section-title wp-kinetic-line">
        <span data-i18n="useCaseCards.titleLead">{t(lang, 'useCaseCards.titleLead')}</span><!--
        --><span class="text-gradient-brand" data-i18n="useCaseCards.titleAccent">{t(lang, 'useCaseCards.titleAccent')}</span><!--
        --><span data-i18n="useCaseCards.titleTail">{t(lang, 'useCaseCards.titleTail')}</span>
      </h2>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 stagger-children">
      {workflows.map(({ key, href, illustration, illustrationAltKey }) => (
        <a
          href={href}
          class="use-case-card stagger-item group"
          aria-label={t(lang, `useCaseCards.${key}.ariaLabel`)}
          data-i18n-aria={`useCaseCards.${key}.ariaLabel`}
        >
          <div class="use-case-card__icon">
            <img
              src={illustration}
              alt={t(lang, illustrationAltKey)}
              width="1024"
              height="1024"
              loading="lazy"
              decoding="async"
              class="use-case-card__illustration"
            />
          </div>
          <h3 class="use-case-card__title"
              data-i18n={`useCaseCards.${key}.title`}>{t(lang, `useCaseCards.${key}.title`)}</h3>
          <p class="use-case-card__desc"
             data-i18n={`useCaseCards.${key}.desc`}>{t(lang, `useCaseCards.${key}.desc`)}</p>
          {/* Dauerhaft sichtbare Navigations-Affordance (statt des früheren
              hover-only-Pfeils): gleiche CTA-Sprache wie der Engines-Link
              ("Zum vollständigen Vergleich →"), damit die Karte auf einen
              Blick als "führt zu einer Unterseite" lesbar ist. */}
          <span class="use-case-card__cta">
            <span data-i18n="useCaseCards.more">{t(lang, 'useCaseCards.more')}</span>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M5 12h14" />
              <path d="m12 5 7 7-7 7" />
            </svg>
          </span>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .use-case-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    position: relative;
    padding: 2rem 1.75rem;
    border-radius: var(--radius-card);
    /* 12% wie alle Strukturkanten der Seite (Flutter borderSubtle, global.css) */
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: var(--color-brand-card);
    box-shadow: var(--shadow-card);
    text-decoration: none;
    color: inherit;
    transition: border-color 0.3s var(--ease-flow), transform 0.3s var(--ease-flow), box-shadow 0.3s var(--ease-flow);
    will-change: transform;
  }

  .use-case-card::after {
    content: "";
    position: absolute;
    inset-inline: 1.25rem;
    top: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, color-mix(in srgb, var(--color-brand-cyan) 55%, transparent), transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
  }

  .use-case-card:hover {
    border-color: color-mix(in srgb, var(--color-brand-cyan) 30%, transparent);
    transform: translateY(-4px);
    box-shadow: var(--shadow-card-hover);
  }

  .use-case-card:hover::after {
    opacity: 1;
  }

  .use-case-card__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 7.5rem;
    height: 7.5rem;
    margin: 0 auto 1.25rem;
    flex-shrink: 0;
    transition: transform 0.3s var(--ease-flow);
  }

  .use-case-card__illustration {
    width: 100%;
    height: 100%;
    object-fit: contain;
    filter: drop-shadow(0 18px 34px rgba(0, 0, 0, 0.28));
  }

  .use-case-card:hover .use-case-card__icon {
    transform: scale(1.08);
  }

  .use-case-card__title {
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--color-ink);
    margin-bottom: 0.5rem;
    line-height: 1.3;
  }

  .use-case-card__desc {
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--color-ink-muted);
    flex: 1;
  }

  /* Ruhende, voll sichtbare CTA-Zeile — Label + Pfeil im etablierten
     Link-CTA-Muster der Site (brand-cyan, semibold). Auf Karten-Hover
     fließt nur der Pfeil auf der Flow-Kurve nach; das Label bleibt stehen. */
  .use-case-card__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    margin-top: 1rem;
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-brand-cyan);
    align-self: center;
  }
  .use-case-card__cta svg {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
    transition: transform 0.25s var(--ease-flow);
  }

  .use-case-card:hover .use-case-card__cta svg {
    transform: translateX(4px);
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .use-case-card,
    .use-case-card::after,
    .use-case-card__icon,
    .use-case-card__cta svg {
      transition: none;
    }

    .use-case-card:hover {
      transform: none;
      box-shadow: none;
    }

    .use-case-card:hover .use-case-card__icon {
      transform: none;
    }

    .use-case-card:hover .use-case-card__cta svg {
      transform: none;
    }
  }
</style>
