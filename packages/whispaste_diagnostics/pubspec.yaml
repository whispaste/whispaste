name: whispaste_diagnostics
description: "Pure-Dart diagnostics core for WhisPaste. No Flutter dependency."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  archive: ^4.0.9
  meta: ^1.16.0
  path: ^1.9.1
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
